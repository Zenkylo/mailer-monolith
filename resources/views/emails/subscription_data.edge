<!DOCTYPE html>
<html>

<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>{{ subscription.name }} - Data Update</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            line-height: 1.6;
            color: #333;
            max-width: 600px;
            margin: 0 auto;
            padding: 20px;
        }

        .header {
            background-color: #f8f9fa;
            padding: 20px;
            border-radius: 8px;
            margin-bottom: 20px;
        }

        .content {
            margin-bottom: 20px;
        }

        .data-section {
            background-color: #f8f9fa;
            padding: 15px;
            border-radius: 5px;
            margin: 15px 0;
        }

        .metadata {
            font-size: 0.9em;
            color: #666;
            border-top: 1px solid #ddd;
            padding-top: 15px;
            margin-top: 20px;
        }

        .error {
            color: #dc3545;
        }

        .success {
            color: #28a745;
        }

        pre {
            background-color: #f1f3f4;
            padding: 10px;
            border-radius: 3px;
            overflow-x: auto;
            white-space: pre-wrap;
            font-size: 0.9em;
        }
    </style>
</head>

<body>
    <div class="header">
        <h1>{{ subscription.name }}</h1>
        <p>Your subscription data has been updated</p>
    </div>

    <div class="content">
        <h2>Latest Data</h2>
        <div class="data-section">
            <h3>Response Data:</h3>
            <pre>{{ JSON.stringify(fetchedData, null, 2) }}</pre>
        </div>

        @if(statusCode)
        <p class="success">✅ Status: {{ statusCode }} (Success)</p>
        @endif
    </div>

    <div class="metadata">
        <h3>Subscription Details</h3>
        <ul>
            <li><strong>Subscription Name:</strong> {{ subscription.name }}</li>
            <li><strong>Endpoint:</strong> {{ subscription.endpoint }}</li>
            <li><strong>Fetched At:</strong> {{ new Date(fetchedAt).toLocaleString() }}</li>
        </ul>
    </div>

    <div class="metadata">
        <p><small>This email was automatically generated by your subscription service. To manage your subscriptions, log
                into your account.</small></p>
    </div>
</body>

</html>